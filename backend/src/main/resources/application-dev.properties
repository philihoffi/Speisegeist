# Dev profile overrides — WARNING: only for local development, NEVER in production.
# These values are only used when no ADMIN_* environment variables are set.
admin.email=admin@speisegeist.local
admin.password=admin123

# Security DEBUG in dev for troubleshooting filter chain
logging.level.org.springframework.security=DEBUG